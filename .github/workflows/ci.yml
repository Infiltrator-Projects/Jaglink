name: JAGLINK CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging:
    name: Release staging guard
    runs-on: ubuntu-latest
    steps:
      - run: echo "Push-triggered CI is temporarily disabled while the 0.2.0 release tree is staged on main."
